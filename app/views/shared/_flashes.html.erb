<% if flash.any? %>
  <% flash.each do |type, msg| %>
    <div class="alert alert-<%= type %>">
      <%= msg %>
    </div>
    <% end %>
<% end %>

<script>
  setTimeout(() => {
    document.querySelector(".alert").classList.add("d-none");
  }, 5000);
</script>
